export type ScrapedPAItem = {
  title: string;
  amount: string;
  price: string;
  baseUrl: string;
  cas?: string;
  link?: string;
};

export type ScrapedPWItem = {
  title: string;
  size: string;
  price: string;
  cas?: string;
  href?: string;
};
